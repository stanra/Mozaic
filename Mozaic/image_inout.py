"""
    Module to handle the cutting/stitching part of the application.
    It exposes two functions : divide (to create the tiles) and regroup (to stitch tiles together)

"""

import Mozaic.image_lib
