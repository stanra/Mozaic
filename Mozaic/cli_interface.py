"""
    Module to define the behaviour when the application is used as a CLI interface.

    The CLI should be handled in the file that will actually be called by the user, but I think it is cleaner to handle
    the main of the work inside the library modules.

"""

import Mozaic.main

